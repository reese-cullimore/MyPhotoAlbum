export interface AlbumResponse {
    albumId: number,
    id: number,
    title: string,
    url: string,
    thumbnailUrl: string
}

export interface AlbumRecord {
    id: number,
    title: string,
}